# Handelsregister Scraper - Production Dependencies
# Mirrors [project.dependencies] in pyproject.toml — kept for Docker layer caching.

# HTTP & scraping
requests>=2.28.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
cloudscraper>=1.2.71
curl_cffi>=0.7.0
brotli>=1.1.0
ddgs>=9.0.0

# Browser-driven scraping (stealth engine + LinkedIn profile fallback).
# Chromium binary must also be installed: `playwright install chromium`.
# See Dockerfile.stealth for the container-side install step.
playwright>=1.57.0
playwright-stealth>=2.0.0

# PDF parsing — AD (Abdruck) register excerpts come as PDFs and contain
# Stammkapital / Gegenstand / Geschäftsanschrift (see processing/ad_capture.py).
pypdf>=5.0.0

# Data processing
python-dateutil>=2.8.0
rapidfuzz>=3.0.0
pyyaml>=6.0.0

# Web UI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.6
aiofiles>=23.0.0

# Scheduling
apscheduler>=3.10.0
sqlalchemy>=2.0.0

# CLI
click>=8.0.0
rich>=13.0.0

# Config
python-dotenv>=1.0.0
